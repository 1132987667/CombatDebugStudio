# 深度评审实现情况审计报告

审计时间: 2026-07-20
审计方法: 直接读取当前代码文件，逐项比对

## §二 P0 问题（3/3 已修复）

### 2.1 `calculateFinalValue` 单位不一致 → ✅ 已修复
- `@deprecated` 标记 ✅ (types.ts:265)
- JSDoc 注明 decimal 单位（0.2 = 20%）vs percentage-point 单位（20 = 20%）✅ (types.ts:266-267)
- 仍为 `export function`，但已标注 `@deprecated`

### 2.2 `ModifierStack.calculate` 缺少 FINAL → ✅ 已修复
- `IModifierStack` 接口中已无 `calculate` 方法
- `ModifierStack` 类中已无 `calculate` 方法

### 2.3 BattleDashboard 响应式 → ✅ 已修复
- `attrVal`: `void char?.statsVersion` (BattleDashboard.vue:295)
- `attackRange`: `void char.statsVersion` (BattleDashboard.vue:329)
- `displayHp`: `void char.statsVersion` (BattleDashboard.vue:275)
- `displayEnergy`: `void char.statsVersion` (BattleDashboard.vue:285)

## §三 P1 问题（6/7 已修复，1 未修复）

### 3.1 双重修饰符存储与同步复杂度 → ⏳ 部分修复
- 核心架构未变：ParticipantStats + ModifierStack 两套存储仍在
- `syncModifiersFromProvider` 仍遍历全部 Object.values(ATTRIBUTE_CODE) (BattleParticipantImpl.ts:171)
- 仍调用 `notifyModifiersChanged()` 使所有属性过期
- passive 识别仍用 `sourceKey.startsWith('passive:')`
- **仅标注了 `@deprecated`，未重构架构**

### 3.2 `recalcAll`/`recalculateAll` 重复 → ✅ 已修复
- `recalculateAll` @deprecated，委托给 `recalcAll` (BattleParticipantImpl.ts:248-253)

### 3.3 命名混淆 → ✅ 已修复
- `getAttrValue` 标记 @deprecated (BattleParticipantImpl.ts:225)

### 3.4 `_statsVersion` 双版本号 → ✅ 已修复
- `_statsVersion` 字段已删除
- `statsVersion` 直接映射到 `stats.getCurrentVersion()` (BattleParticipantImpl.ts:100-102)
- health/energy setter 补上 `notifyModifiersChanged()`

### 3.5 `pushModifier` minAttack/maxAttack 不对称 → ✅ 已修复
- PERCENTAGE 同步到 minAttack/maxAttack ✅ (GameDataProcessor.ts:263-279)
- ADDITIVE 同步到 minAttack/maxAttack ✅ (GameDataProcessor.ts:282-298)

### 3.6 `useParticipantStats` computed 不响应 → ✅ 已修复
- 全部 10 个 computed 增加 `void participant.statsVersion` (useParticipantStats.ts:120-158)

### 3.7 `AttributeMetaMap` 包含 UI 层概念 → ❌ **未修复**
- `displayTier` 仍在 `AttributeMeta` 接口 (types.ts:339)
- `group` 仍在 `AttributeMeta` 接口 (types.ts:341)
- `AttributeMetaMap` 中每个条目仍包含这两个字段

## §四 P2 问题（5/5 已修复）

### 4.1 `AttributeEngine` 名不副实 → ✅ 已修复（文件已删除）
### 4.2 9 个未用类型 → ✅ 已修复（8 个已删除，CalculationStep 保留）
### 4.3 未用 ATTRIBUTE_CODE → ✅ 已修复（5 个已删除）
### 4.4 `breakdown` 未填充 → ✅ 已修复（ParticipantStats.ts:144-150）
### 4.5 `ModifierStack.calculate` 重复 → ✅ 已修复（已删除）

## §五 链路追踪 → 已全部验证
- 被动技能路径: pushModifier → recalcAll → syncModifiersFromProvider → recalculateAll → _statsVersion++
  ✅ 唯一变化: _statsVersion 已统合到 stats.version
- Buff 路径: addBuff → ModifierStack → eventBus → battleStore → proxy.recalculateAll()
  ✅ 唯一变化: recalculateAll 现在委托给 recalcAll

## §六 汇总表 → 13/15 已修复, 1 部分, 1 未修复

## §七 修复建议 → 6/7 已执行, 1 未执行
- ① 标注 `calculateFinalValue` 单位 → ✅ @deprecated + JSDoc
- ② BattleDashboard `void statsVersion` → ✅
- ③ 删除 `ModifierStack.calculate` → ✅
- ④ 合并 `recalcAll`/`recalculateAll` → ✅
- ⑤ 统一属性访问 API → ✅ `getAttrValue` @deprecated
- ⑥ 优化 `syncModifiersFromProvider` → ⏳ 延期
- ⑦ 移出 `displayTier`/`group` → ❌ **未执行**

## §八 事实面更新
| 事实面 | 原状态 | 现状态 |
|--------|--------|--------|
| 代码 | changed-and-verified | ✅ **resolved** |
| 文档 | stale | ⚠️ 仍 stale（未更新文档） |
| 规则 | stale | ⚠️ 仍 stale（AttributeMetaMap 未迁移） |
| 运行态 | pending | ✅ **verified**（48/48 测试通过） |
| 工作区 | changed-and-verified | ✅ **resolved** |
