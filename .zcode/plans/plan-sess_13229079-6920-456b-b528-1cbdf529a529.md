# 封神榜数值体系扩展功能规划.md —— 评审偏差修复计划

按你的 4 项决策修正文档，全部改动限于 `documents/封神榜数值体系扩展功能规划.md` 一个文件（Edit 保留 UTF-8 BOM）。

## 1. 偏差1：talent 枚举 —— 保持 7 个（记录排除决策）
- 「九、开放问题」新增一条：代码 `SystemBudgetEntry`（types.ts:344）含 talent，但系统投放配置**有意排除**（7 系统）；若未来天赋参与投放需同步扩展 3.1 枚举
- 3.1 两处 systems 注释（64 / 207 行）标注「有意不含 talent，见开放问题」

## 2. 偏差2：3.7 收敛为 ExpGoldView 增补
- **3.7 整节改写**：标题改「3.7 经验曲线增补（P2，收敛）」；不新建 ExpCurveView，改为在已有 ExpGoldView（升级经验折线 + 肝度曲线已存在）上增补：① 关键节点标注（转职/觉醒/满级）② 累计经验曲线；删除原「拖动曲线调参」等与现状重叠的描述
- 联动修改 4 处引用：
  - 四章优先级表 3.7 行理由 → 「核心已被 ExpGoldView 覆盖，仅剩标注增补」
  - 5.1 视图清单删除 `ExpCurveView.vue`
  - 六章 Phase 3 工作量 2-3 天 → 1-1.5 天（标注增补 + 掉落模拟器）
  - 八章验收标准 3.7 行 → 对应增补项

## 3. 偏差3：测试数字改基线表述
- 八章「回归底线」句改为：「全量测试相对当前基线无新增失败（已知存量失败与 flaky 不计入）」，不写死数字

## 4. 偏差4：5.6 收窄为配置生成侧
- **5.6 节改写**：开头分域说明——① 战斗内随机已有 seed→battleData.rng 机制 + 确定性契约测试（tests/unit/seeded-battle-determinism.test.ts），3.11 蒙特卡洛直接复用，不在本节范围；② 本节 Rng 基建只覆盖装备生成（3.2）与掉落模拟（3.8）
- 3.11 实现约束补一句：模拟种子经引擎 battleData.rng 注入（每场用 seed+i 派生），不再暗示需要独立 Rng

## 5. 顺带补正（评审发现，无需再确认）
- 3.9 处补一句现状锚点：AuditView 已有预算权重条 + 词条频次展示，本功能在其上补 SAP 换算与偏差判定
- 修订记录表补一行「2026-09-08 评审修正」：列出本次 4 项决策结果
- 完成后验证文件头 3 字节仍为 EF BB BF（BOM 保留）

## 验证
- grep 确认无残留 ExpCurveView 引用、无「114 个测试」残留
- 通读改动节与上下文编号/交叉引用一致