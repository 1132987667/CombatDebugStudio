# Buff 系统遗留问题清单

> 本文档源自《修改.md》（Buff 系统审计与架构修正记录，2026-07-30+）。审计修复与架构决策（PATH A/B/D 规则）均已落地并归档（见提交历史与代码注释），此处仅保留**未修复/待确认**事项。复核日期：2026-08-11。
> **2026-08-11 复核**：原 7.4「战斗结束后立即重置」已确认可通过手动路径达成（`ControlBar` 重置按钮在战斗结束后可用，重置 → 开始战斗即初始化双方状态；录像已持久化，重置只清内存副本），该条从清单移除。

## 一、未修复事项

### 1.1 概念属性无消费代码（原 7.2）

21 个概念属性（免疫判定、元素伤害、连击/反击）仅定义无消费逻辑。

**状态**：⚠️ 仍存在
**说明**：`src/domain/attribute/attribute-codes.generated.ts` 定义 + MetaMap，全库无消费逻辑

### 1.2 EffectType 消费方迁移未完成（原 7.3）

**状态**：⚠️ 仍存在
**说明**：42 个文件仍引用 `@deprecated` 的 `EffectType`（记录时 31 个，2026-08-05 复核时 41 个，当前 42 个）；仅 `SkillStep.type` 收窄为 `StepEffectType`

### 1.3 4 级配置回退链（原 P3-10）

**状态**：⚠️ 有意保留（非缺陷）
**说明**：`addBuff` 字段回退链（调用方 → 脚本 CONFIG → JSON → 硬编码）调试追踪成本高；如需改进，抽取配置构建器（可选）
