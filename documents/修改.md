cloneParticipant


if (config.effectPlan && config.effectPlan.length > 0) {
    for (const effect of config.effectPlan) {
      switch (effect.type) {
        case 'modifier': facets.push(StatusCategory.MODIFIER); break
        case 'dot':      facets.push(StatusCategory.DOT); break
        case 'heal':      facets.push(StatusCategory.TRIGGER); break  // HEAL 归入 trigger 类
        case 'control':  facets.push(StatusCategory.CONTROL); break
        case 'shield':   facets.push(StatusCategory.SHIELD); break
        case 'trigger':  facets.push(StatusCategory.TRIGGER); break
        case 'aura':     facets.push(StatusCategory.AURA); break
        case 'immunity': facets.push(StatusCategory.IMMUNITY); break
      }
    }
    // 去重
    return [...new Set(facets)]
  }

  config.effectPlan  有这个 effectPlan 字段么

  buffs.json 是否有对应的接口定义