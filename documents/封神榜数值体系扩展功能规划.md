> **状态**：规划中（2026-09-01）。待评审确认后按 Phase 1-3 推进。

## 一、背景

封神榜已完成基础数值体系配置（玩家成长/系统预算/装备公式/属性定义/来源审计），但策划在实际工作中仍面临以下痛点：

1. **改参无预览**：调了数值不知道全局影响，容易改崩
2. **装备手填累**：每件装备都要手动配属性，效率低
3. **校验靠肉眼**：平衡性问题只能等实际测试才发现
4. **配置易出错**：没有约束，容易配出超标数据

本规划旨在补齐这些能力，让封神榜成为策划的**数值控制中心**。

---

## 二、数值体系全链路分析

### 2.1 当前链路状态

```
① 度量衡      属性定义（SAP 价值倍数 / L1-L4 / 归属系统）
     ↓
② 玩家成长    玩家配置（初始 → 成长 → 自由点 → 转化 → 预览）
     ↓
③ 系统投放    等级 / 装备 / 流派 / 宠物 / 坐骑 / 法宝 / 神器
     ↓
④ 来源审计    属性 × 系统矩阵（只读）
     ↓
⑤ 公式结算    属性与公式（只读参考）
```

### 2.2 已有 vs 缺失

| 环节 | 已有 | 缺失 |
|------|------|------|
| ① 度量衡 | attributes 表（SAP/层级/归属） | 属性上限/下限约束、属性间依赖关系 |
| ② 玩家成长 | PlayerConfigView（六维配置+经验公式） | 转职/觉醒等特殊成长路径 |
| ③ 系统投放 | SystemBudget 权重（只读） | **各系统实际属性值配置**、**装备词条数值范围**、**丹药/洗练/强化效果** |
| ④ 来源审计 | AuditView 矩阵（只读） | **一键校验**（实际投放 vs 预算偏差） |
| ⑤ 公式结算 | FormulasView（只读） | 实时预览（调参后立即看到战斗效果） |

### 2.3 建设性方案（4 个扩展维度）

#### 方案 A：系统投放明细配置

**目标**：为每个养成系统配置「该系统能投放哪些属性、投放范围、投放规则」

```ts
interface SystemDistributionConfig {
  id: 'system_distribution'
  systems: Array<{
    system: string              // level / equipment / school / pet / mount / artifact / relic
    label: string
    distributions: Array<{
      attribute: string         // 属性代码（引用 attributes 表）
      mode: 'fixed' | 'range' | 'formula'  // 投放模式
      value?: number            // fixed 模式：固定值
      range?: { min: number; max: number }  // range 模式：随机区间
      formula?: string          // formula 模式：公式（如 'level * 2 + 10'）
      weight?: number           // 抽池权重（用于随机生成）
    }>
  }>
}
```

**视图**：`SystemDistributionView.vue`
- 顶部：选择系统（下拉）→ 该系统预算权重 + 已投放属性汇总
- 主体：属性投放配置表（属性/模式/值/范围/公式）
- 底部：一键校验（实际配置 vs 预算偏差）

#### 方案 B：装备词条数值范围控制

**目标**：统一控制词条数值的生成规则，而不是每个词条单独配 valueRange

```ts
interface AffixDistributionConfig {
  id: 'affix_distribution'
  /** 按稀有度定义词条数值范围倍率 */
  rarityMultiplier: Record<number, { min: number; max: number }>  // 1~5 稀有度 → 范围倍率
  /** 按品阶定义词条数值范围倍率 */
  tierMultiplier: Record<string, { min: number; max: number }>    // t1~t5 品阶 → 范围倍率
  /** 全局词条数值上下限 */
  globalClamp: { min: number; max: number }
  /** 词条数量规则（按装备稀有度/品阶决定可携带词条数） */
  affixCountRules: Array<{
    condition: { rarity?: number; tier?: string }
    count: { min: number; max: number }
  }>
}
```

**视图**：嵌入 `EquipmentView` 或独立视图
- 稀有度倍率表
- 品阶倍率表
- 词条数量规则
- 一键校验：扫描所有 equipment_affixes，检查 valueRange 是否在全局约束内

#### 方案 C：丹药/消耗品效果配置

**目标**：将丹药效果从硬编码转为可配置

```ts
interface PillEffectConfig {
  id: 'pill_effect'
  /** 丹药列表（每个丹药 = 一次属性点投放） */
  pills: Array<{
    id: string
    name: string
    /** 提供的属性点 */
    bonusPoints: number
    /** 使用等级门槛 */
    requiredLevel: number
    /** 获取来源（掉落/商店/任务） */
    source: string
    /** 稀有度 */
    rarity: number
  }>
  /** 丹药总上限（所有丹药提供的属性点总和不能超过此值） */
  totalCap: number
}
```

**视图**：`PillEffectView.vue` 或嵌入 PlayerConfigView
- 丹药列表（可增删改）
- 已使用属性点 / 剩余可用属性点
- 一键校验：丹药总属性点是否超过 pillBonusPoints

#### 方案 D：平衡性校验引擎

**目标**：一键校验整个数值体系的平衡性，输出报告

```ts
interface BalanceReport {
  /** 各系统实际投放 vs 预算偏差 */
  systemDeviations: Array<{
    system: string
    budgetWeight: number      // 预算权重（660 口径）
    actualSap: number         // 实际投放 SAP
    deviation: number         // 偏差百分比
    status: 'ok' | 'warn' | 'error'
  }>
  /** 属性覆盖检查：哪些属性没有主来源 */
  uncoveredAttributes: string[]
  /** 数值范围检查：装备词条是否超出全局约束 */
  outOfRangeAffixes: string[]
  /** 丹药检查：总属性点是否超过上限 */
  pillOverCap: boolean
  /** 总体平衡评分（0-100） */
  balanceScore: number
}
```

**视图**：`BalanceCheckView.vue`（独立视图或嵌入 AuditView）
- 雷达图：各系统投放占比
- 偏差条：实际 vs 预算
- 问题列表：点击跳转到对应配置修复

---

## 三、落地功能清单

### 3.1 系统投放明细配置（P0）

**场景**：策划需要精确控制每个养成系统实际投放什么属性、多少数值

**功能**：
- 为每个系统（等级/装备/流派/宠物/坐骑/法宝/神器）配置投放规则
- 每条规则：属性代码 + 投放模式（固定值/随机区间/公式）
- 支持按等级/品阶动态计算投放值
- 一键校验：实际配置 vs 预算偏差

**输入**：系统选择 + 属性投放规则

**输出**：
- 该系统的属性投放配置表
- 该系统预算权重 + 已投放 SAP 汇总
- 偏差预警（实际投放超出预算 10%）

**价值**：策划能精确控制每个系统的数值投放，避免"拍脑袋"配置

**数据结构**：
```ts
interface SystemDistributionConfig {
  id: 'system_distribution'
  systems: Array<{
    system: string              // level / equipment / school / pet / mount / artifact / relic
    label: string
    distributions: Array<{
      attribute: string         // 属性代码（引用 attributes 表）
      mode: 'fixed' | 'range' | 'formula'
      value?: number            // fixed 模式
      range?: { min: number; max: number }  // range 模式
      formula?: string          // formula 模式（如 'level * 2 + 10'）
      weight?: number           // 抽池权重
    }>
  }>
}
```

---

### 3.2 装备批量生成器（P0）

**场景**：策划需要批量生成装备来测试数值

**功能**：
- 按公式生成核心属性值（含浮动区间）
- 按词条池随机生成附加属性
- 输出完整装备 JSON（可直接导入 equipment 表）
- 批量生成 N 件同等级装备，统计数值分布

**输入**：装备等级 + 品阶 + 部位 + 数量

**输出**：
- 装备列表（JSON 格式）
- 数值分布统计（平均值/最大值/最小值）
- 一键导入封神榜

**价值**：策划不用手填装备数据，公式驱动自动生成

---

### 3.3 属性上限约束器（P0）

**场景**：策划需要约束属性不要无限膨胀

**功能**：
- 每个属性的合理范围（min/max）配置
- 玩家满级属性是否超标检查
- 装备词条数值是否超标检查
- buff 叠加后是否超标检查
- 保存时自动拦截超标数据

**输入**：属性上下限配置

**输出**：
- 校验结果（超标列表）
- 保存拦截提示

**价值**：防止后期数值爆炸，保持游戏平衡

---

### 3.4 消耗品效果配置（P1）

**场景**：策划需要调整丹药/洗练/强化等消耗品的效果

**功能**：
- 丹药配置：每个丹药提供的属性点、使用等级门槛、获取来源
- 洗练配置：各部位可洗练的属性范围、数值区间
- 强化配置：强化等级对应的属性加成百分比
- 一键校验：丹药总属性点是否超过上限

**输入**：消耗品类型 + 效果参数

**输出**：
- 消耗品配置表
- 已使用属性点 / 剩余可用属性点
- 校验结果

**价值**：策划能灵活调整消耗品效果，不用改代码

**数据结构**：
```ts
interface ConsumeEffectConfig {
  id: 'consume_effect'
  pills: Array<{
    id: string
    name: string
    bonusPoints: number
    requiredLevel: number
    source: string
    rarity: number
  }>
  refineRules: Array<{
    targetSlot: string
    attributeRanges: Record<string, { min: number; max: number }>
  }>
  enhanceRules: Array<{
    level: number
    bonusPercent: number
  }>
  totalPillCap: number
}
```

---

### 3.5 敌人属性校验器（P1）

**场景**：策划配了一堆敌人，需要校验是否符合等级曲线

**功能**：
- 每个敌人的属性是否在合理区间（偏高/偏低）
- 掉落经验/金钱是否符合预期曲线
- 敌人 vs 同等级玩家的胜率估算
- 异常数据高亮（如 99999 血的 1 级怪）

**输入**：敌人列表 + 玩家等级曲线

**输出**：
- 校验报告（异常列表）
- 属性分布图
- 点击跳转修复

**价值**：防止策划配出"秒杀 boss"或"打不死的 1 级怪"

---

### 3.6 数值快照对比器（P1）

**场景**：策划改了一版数值，想对比"改前 vs 改后"的差异

**功能**：
- 玩家满级属性对比（六维数值 + SAP 总量）
- 各系统投放占比对比（雷达图）
- 装备生成数值对比（10级/30级/50级各品阶）
- 差异高亮（哪些属性变化超过 10%）

**输入**：两套配置（A/B）

**输出**：
- 属性对比表（差异列高亮）
- 雷达图对比
- 差异报告

**价值**：策划调参后立即看到全局影响，避免"改了一个数崩了整个体系"

---

### 3.7 经验曲线可视化（P2）

**场景**：策划需要调整升级节奏

**功能**：
- 折线图：等级 vs 所需经验
- 折线图：等级 vs 累计经验
- 标注：关键节点（转职/觉醒/满级）
- 交互：拖动曲线实时更新公式参数

**输入**：经验公式 + 最高等级

**输出**：
- 经验曲线图
- 关键节点标注
- 公式参数调整

**价值**：策划直观看到升级节奏，不用心算公式

---

### 3.8 掉落模拟器（P2）

**场景**：策划需要验证掉落率是否合理

**功能**：
- 模拟 N 次掉落
- 统计每种物品的获得次数
- 稀有物品的期望掉落率
- 模拟分布图

**输入**：掉落组 + 模拟次数

**输出**：
- 掉落统计表
- 分布图
- 与策划预期对比

**价值**：不用实际跑游戏就能验证掉落设计

---

### 3.9 平衡性校验引擎（P1）

**场景**：策划需要一键校验整个数值体系是否平衡

**功能**：
- 各系统实际投放 vs 预算偏差检查
- 属性覆盖检查：哪些属性没有主来源
- 数值范围检查：装备词条是否超出全局约束
- 丹药检查：总属性点是否超过上限
- 总体平衡评分（0-100）

**输入**：全部配置数据

**输出**：
- 平衡性报告（雷达图 + 偏差条 + 问题列表）
- 一键跳转修复

**价值**：一键发现问题，指导调整，避免"改了一个数崩了整个体系"

**数据结构**：
```ts
interface BalanceReport {
  systemDeviations: Array<{
    system: string
    budgetWeight: number
    actualSap: number
    deviation: number
    status: 'ok' | 'warn' | 'error'
  }>
  uncoveredAttributes: string[]
  outOfRangeAffixes: string[]
  pillOverCap: boolean
  balanceScore: number
}
```

---

### 3.10 战斗力估算器（P3）

**场景**：策划需要估算玩家在不同养成阶段的战斗力

**功能**：
- 预估战斗力数值
- 与同等级怪物的对比
- 推荐挑战的关卡
- 养成瓶颈分析（哪个系统拖后腿）

**输入**：玩家等级 + 养成进度（装备/流派/宠物...）

**输出**：
- 战斗力数值
- 对比报告
- 养成建议

**价值**：策划从玩家角度审视数值体验

---

## 四、优先级排序

按**实际价值 + 使用频率 + 实现成本**排序：

| 优先级 | 功能 | 覆盖环节 | 理由 |
|--------|------|----------|------|
| **P0** | 系统投放明细配置 | ③ 系统投放设计 | 核心缺失——目前只有权重没有实际投放量 |
| **P0** | 装备批量生成器 | ④ 装备数值设计 | 每次配装备都要用，无替代方案 |
| **P0** | 属性上限约束器 | ⑧ 平衡性校验 | 保存时自动拦截，防止数值爆炸 |
| **P1** | 消耗品效果配置 | ⑤ 消耗品设计 | 丹药/洗练/强化效果硬编码，策划无法调整 |
| **P1** | 平衡性校验引擎 | ⑧ 平衡性校验 | 一键校验整体平衡性，发现问题 |
| **P1** | 敌人属性校验器 | ⑥ 敌人设计 | 配敌人后检查一次，防止配置错误 |
| **P1** | 数值快照对比器 | ⑩ 版本管理 | 改参时对比差异，防止改崩 |
| **P2** | 经验曲线可视化 | ② 玩家成长设计 | 直观但非刚需，Excel 能替代 |
| **P2** | 掉落模拟器 | ⑦ 经济系统 | 调掉落率时用，频率不高 |
| **P3** | 战斗力估算器 | ⑨ 实时预览 | 依赖上述配置完成，锦上添花 |

```
高频 + 无替代 → P0（系统投放配置、装备生成器、属性约束器）
中频 + 有价值 → P1（消耗品配置、平衡性校验、敌人校验器、快照对比器）
低频 + 可替代 → P2（经验曲线、掉落模拟器）
依赖其他 + 锦上添花 → P3（战斗力估算器）
```

---

## 五、技术实现建议

### 5.1 架构分层

```
纯函数层（domain/fengshen/）
├── equip-generator.ts        # 装备生成逻辑
├── system-distributor.ts     # 系统投放计算
├── balance-checker.ts        # 平衡性校验
├── consume-effect.ts         # 消耗品效果计算
├── snapshot-diff.ts          # 快照对比
├── drop-simulator.ts         # 掉落模拟
└── attribute-validator.ts    # 属性校验

视图层（presentation/modules/fengshen/views/）
├── SystemDistributionView.vue  # 系统投放配置
├── EquipGeneratorView.vue      # 装备生成器
├── AttributeLimitView.vue      # 属性上限配置
├── ConsumeEffectView.vue       # 消耗品配置
├── BalanceCheckView.vue        # 平衡性校验
├── EnemyValidatorView.vue      # 敌人属性校验
├── SnapshotCompareView.vue     # 快照对比
├── ExpCurveView.vue            # 经验曲线
├── DropSimulatorView.vue       # 掉落模拟
└── PowerEstimatorView.vue      # 战斗力估算
```

### 5.2 数据流

```
配置（params 域 JSON）
    ↓
纯函数计算（domain 层，无 IO、可单测）
    ↓
校验/模拟/生成
    ↓
结果展示 + 一键导入
```

### 5.3 复用模式

- **纯函数**：对齐 `player-config.ts` 模式（无 IO、可单测）
- **视图**：对齐 `PlayerConfigView.vue` 模式（Tab + 草稿 + 保存）
- **种子**：对齐 `buildPlayerConfig()` 模式（params 域结构化表）
- **校验**：对齐 `DataIntegrityService` 模式（REFERENCE_RULES + 强校验）

### 4.4 数据结构

所有新配置统一落入 `params` 域，遵循已有模式：

```ts
// types.ts 新增
interface EquipGeneratorConfig {
  id: 'equip_generator'
  defaultLevel: number
  defaultTier: string
  defaultSlot: string
  generateCount: number
}

interface AttributeLimitConfig {
  id: 'attribute_limit'
  limits: Record<string, { min: number; max: number }>
}

interface BalanceCheckConfig {
  id: 'balance_check'
  /** 属性合理区间（按等级） */
  attributeRanges: Array<{
    level: number
    ranges: Record<string, { min: number; max: number }>
  }>
  /** 敌人属性倍率（相对于同等级玩家） */
  enemyMultiplier: { min: number; max: number }
}
```

---

## 六、落地路径

| 阶段 | 功能 | 工作量 | 依赖 |
|------|------|--------|------|
| **Phase 1** | 系统投放明细配置 + 装备批量生成器 + 属性上限约束器（P0） | 4-5 天 | SystemBudget + EquipFormula + attributes |
| **Phase 2** | 消耗品效果配置 + 平衡性校验引擎 + 敌人属性校验器 + 数值快照对比器（P1） | 5-6 天 | PlayerConfig + enemies + drops |
| **Phase 3** | 经验曲线可视化 + 掉落模拟器（P2） | 2-3 天 | ExpTable + drops |
| **Phase 4** | 战斗力估算器（P3） | 2-3 天 | 依赖 Phase 1-3 完成 |

### Phase 1 详细任务

| 任务 | 文件 | 说明 |
|------|------|------|
| 系统投放配置 | `params` 域 `system_distribution` | 新结构化表 |
| 系统投放视图 | `views/SystemDistributionView.vue` | 选择系统 + 配置属性投放规则 |
| 装备生成纯函数 | `domain/fengshen/equip-generator.ts` | 按公式批量生成装备 |
| 装备生成视图 | `views/EquipGeneratorView.vue` | 配置参数 + 预览 + 一键导入 |
| 属性上限配置 | `params` 域 `attribute_limit` | 新结构化表 |
| 属性校验逻辑 | `domain/fengshen/attribute-validator.ts` | 校验属性是否超标 |
| 菜单注册 | `Fengshen.vue` | SYSTEM_VIEWS 新增三项 |
| 单测 | `tests/unit/fengshen-equip-generator.test.ts` | 复用 MemoryStorage 模式 |

---

## 七、风险与注意

| 风险 | 应对 |
|------|------|
| 系统投放配置可能与现有预算冲突 | 实时计算偏差，保存时拦截超标 |
| 装备生成器可能生成非法数据 | 复用 DataIntegrityService 校验，保存前拦截 |
| 消耗品配置可能超出属性上限 | 保存时自动校验，拦截超标数据 |
| 快照对比器需要保存历史版本 | 利用已有的 dataVersion 机制，每次保存前备份 |
| 属性上限可能与现有装备/词条冲突 | 提供"自动修正"功能，将超标值钳制到上限 |
| 掉落模拟需要随机数 | 使用固定种子，保证可复现 |

---

## 八、验收标准

| 功能 | 验收标准 |
|------|----------|
| 系统投放明细配置 | 选择"装备"系统，配置 3 条属性投放规则，实时显示偏差 |
| 装备批量生成器 | 生成 100 件 50 级仙品武器，核心属性在公式计算区间内 |
| 属性上限约束器 | 保存时拦截超标属性，提示具体字段 |
| 消耗品效果配置 | 配置丹药提供 12 点属性，使用等级 50，总点数不超上限 |
| 平衡性校验引擎 | 一键校验后输出报告，偏差>15%的系统标红 |
| 敌人属性校验器 | 能发现 99999 血的 1 级怪并高亮 |
| 数值快照对比器 | 对比两套配置，差异高亮准确，无遗漏 |
| 经验曲线可视化 | 曲线图正确反映公式，关键节点标注清晰 |
| 掉落模拟器 | 模拟 1000 次掉落，统计结果与理论值偏差 < 5% |
| 战斗力估算器 | 估算结果与实际战斗误差 < 10% |
