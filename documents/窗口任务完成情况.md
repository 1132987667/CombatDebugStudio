# 窗口任务完成情况

> 更新日期：2026-07-10
> 对应计划：`documents/项目计划书-三个月.md`

---

## 当前窗口：第 1 周（止血与加固）

### 任务 1.1 — 修复 BattleE2E 测试

| 项目 | 内容 |
|------|------|
| **状态** | ✅ 已完成 |
| **根因** | `BattleSystem.initialize()` 在完成所有准备工作后未将战斗状态从 `PREPARING` 切换到 `ACTIVE` |
| **修复** | `src/domain/battle/BattleSystem.ts`：在 `return` 前一行的位置添加 `battleData.battleState = BattleStatus.ACTIVE` |
| **改动量** | 1 文件，+1 行 |
| **验证** | `npm run test` → 5 文件 38 用例全绿 |
| **commit** | — |

### 任务 1.2 — 开启 `strict: true`

| 项目 | 内容 |
|------|------|
| **状态** | ✅ 已完成 |
| **改动** | `tsconfig.json` 中 `strict: false` → `strict: true`，所有子选项从 `false` 改为 `true`（设为等效于 `true` 的值，未删除，保持显式可读） |
| **修复总量** | 约 **30+ 文件，160+ 处类型错误** |
| **验证** | `npm run typecheck` → 零错误 |

#### 修复明细

| 类别 | 文件 | 修复内容 |
|------|------|---------|
| 编码损坏 | `src/infrastructure/input/KeybindManager.ts` | 中文句号被吞导致注释和代码连行，20+ 处语法错误 |
| 接口缺失 | `src/domain/battle/types.ts` | BattleEntity 添加 `currentHealth`/`maxHealth`/`currentEnergy`/`maxEnergy` 属性 |
| import 错误 | `src/domain/battle/service/ActionExecutor.ts` | `BattleTriggerPhase` 误用 `import type` → `import` |
| import 错误 | `src/domain/skill/PassiveSkillManager.ts` | 同上 |
| import 错误 | `src/application/facade/BattleFacade.ts` | `PARTICIPANT_SIDE` 误用为类型；缺少 `ParticipantSide` import |
| import 错误 | `src/domain/port/logging/index.ts` | ILogger 导出方式修正（import + export 分离） |
| 重复声明 | `src/domain/buff/BuffSystem.ts` | 移除重复的 `IModifierProvider`/`ModifierSourceType` import |
| 索引签名 | `src/domain/skill/types.ts` | `SkillSet` 添加 `[key: string]` 索引签名（9 处报错） |
| 缺失属性 | `src/domain/skill/types.ts` | createSkill 返回对象添加 `steps` 属性 |
| null/undefined | `src/domain/battle/service/BattleExecutor.ts` | `?? 0` 处理可选的 damage/heal（10 处） |
| null/undefined | `src/domain/battle/service/ActionExecutor.ts` | `?? 0` + 类型断言 |
| null/undefined | `src/domain/skill/SkillExecutor.ts` | `?? 0` 处理可选属性 |
| null/undefined | `src/domain/battle/service/BattleRecorder.ts` | null → undefined，添加 `checksum` 字段 |
| null/undefined | `src/domain/battle/events/BattleEventManager.ts` | store 类型、htmlResult 断言、source 守卫（5 处） |
| null/undefined | `src/infrastructure/adapters/logging/BattleLogManager.ts` | null → undefined，source/target 守卫 |
| null/undefined | `src/presentation/stores/battleStore.ts` | null → undefined，`!` 断言（8 处） |
| 缺少模块 | `src/domain/env.d.ts`（新建） | Vue SFC 模块声明 |
| 返回类型 | `src/domain/battle/BattleSystem.ts` | `startBattle` → `Promise<void>` |
| 缺少方法 | `src/domain/battle/BattleSystem.ts` | 实现 `getEnabledAllyTeam`/`getEnabledEnemyTeam`/`executeAction` |
| 错误导入 | `src/presentation/stores/battleStore.ts` | `BattleParticipantImpl` 路径修正 |
| 调用签名 | `src/presentation/stores/battleStore.ts` | `addSystemLog` 7 参数 → 对象参数 |
| 类型缺失 | `src/shared/types/battle-log.ts` | 两个返回对象添加 `index`/`type`/`message` |
| 类型断言 | `src/domain/battle/ai/BattleAI.ts` | `(participant as any).isSkillAvailable`，`EFFECT_TYPES.SKILL` → `SPECIAL` |
| 类型断言 | `src/domain/battle/ai/AIPriorityStrategy.ts` | `as unknown as BattleAnalysis`，`(battleAnalysis as any).hasLowHealthAlly` |
| 类型断言 | `src/domain/battle/entity/BattleParticipantImpl.ts` | `SkillType.ALL as 'all'` |
| 类型断言 | `src/domain/battle/entity/ParticipantSkills.ts` | 同上 |
| 类型断言 | `src/infrastructure/di/Container.ts` | `as BattleManager` |
| 缺失导入 | `src/domain/battle/BattleManager.ts` | 添加 `ParticipantSide` 导入 |
| JS 模块 | `src/domain/battle/entity/ParticipantStats.ts` | 移除无效的 Utils import，内联 `Math.round` |
| 移除 token | `src/infrastructure/di/Container.ts` | 移除不存在的 `PARTICIPANT_MANAGER_TOKEN` |
| 不相等比较 | `src/shared/utils/GameDataProcessor.ts` | 移除 `'MODIFY_ATTRIBUTE'` 不可能的比较 |

#### 已知遗留问题

| 问题 | 影响 | 后续 |
|------|------|------|
| domain 层 7 个文件 `import { eventBus } from '@/main'`（层污染） | 不影响运行，但 domain 依赖了入口文件 | 适合抽出独立 `shared/event-bus.ts` 模块 |
| `src/presentation/` 剩余 ~8 个 Vue 相关错误 | 不被 `tsconfig.strict.json` 检查（不在 include 范围） | 需要单独修复或加入严格配置 |
| `env.d.ts` 放在 `src/domain/` 下才能被 strict 配置发现 | 结构上不优雅 | 可以考虑修改 `tsconfig.strict.json` 的 include 模式 |

---

### 任务 1.3 — 战斗日志系统 Phase 1：底层基础设施

| 项目 | 内容 |
|------|------|
| **状态** | ✅ 已完成 |
| **设计参考** | `documents/combat-log-system-design.md` 第 4 节「底层：技术调试日志」 |
| **改动** | 4 新建 + 3 修改 |

**新建文件：**

| 文件 | 用途 |
|------|------|
| `src/shared/types/log-renderer.ts` | `PlayerLogRenderer` 接口 |
| `src/shared/types/trace-log.ts` | `TraceLogEntry` 接口 + 工厂函数 |
| `src/domain/battle/logs/TraceDamageLogger.ts` | 将 `DamageBreakdown` 转为 DEBUG 摘要 + TRACE 树状链路 |
| `src/domain/battle/logs/BuffTraceLogger.ts` | Buff 施加/移除/修饰符变更的 TRACE 日志 |

**修改文件：**

| 文件 | 改动 |
|------|------|
| `src/shared/types/battle-log.ts` | `LogSegment` 增加 `symbol`/`style` 字段 |
| `src/domain/battle/service/BattleExecutor.ts` | 2 处集成 `TraceDamageLogger.log()` |
| `src/domain/buff/BuffSystem.ts` | 3 处集成 `BuffTraceLogger` |

**验证：** `npm run typecheck` → 0 新增错误

---

### 任务 1.4 — 战斗日志系统 Phase 2：玩家日志模板

| 项目 | 内容 |
|------|------|
| **状态** | ✅ 已完成 |
| **设计参考** | `documents/combat-log-system-design.md` 第 5 节「中间层：玩家战斗日志」 |
| **改动** | 2 新建 + 3 修改 |

**新建文件：**

| 文件 | 用途 |
|------|------|
| `src/domain/battle/logs/renderers/PokemonStyleRenderer.ts` | 纯中文叙事风格（默认） |
| `src/domain/battle/logs/renderers/SlayTheSpireRenderer.ts` | 极简数据风格 |

**修改文件：**

| 文件 | 改动 |
|------|------|
| `src/shared/types/log-renderer.ts` | `render()` 增加可选 `BattleAction` 参数 |
| `src/infrastructure/adapters/logging/BattleLogManager.ts` | 新增 `setPlayerRenderer()` / `addPlayerLog()` / 渲染器切换 |
| `src/infrastructure/adapters/logging/index.ts` | 导出渲染器和符号表 |
| `src/presentation/views/BattleLog.vue` | 风格下拉切换 + 底部统计条 |

**验证：** `npm run typecheck` → 0 新增错误

---

### 任务 1.5 — 战斗日志系统 Phase 3：战报系统

| 项目 | 内容 |
|------|------|
| **状态** | ✅ 已完成 |
| **设计参考** | `documents/combat-log-system-design.md` 第 6 节「顶层：战报与复盘」 |
| **改动** | 3 新建 + 4 修改 |

**新建文件：**

| 文件 | 用途 |
|------|------|
| `src/shared/types/battle-summary.ts` | `BattleSummary` / `BattleSummaryAccumulator` 类型 |
| `src/domain/battle/logs/BattleSummaryGenerator.ts` | 战斗过程数据累积 + 战报生成 |
| `src/presentation/views/components/BattleSummaryDialog.vue` | 战报弹窗：统计卡片 + 时间线 + 复制/导出 |

**修改文件：**

| 文件 | 改动 |
|------|------|
| `src/domain/battle/service/BattleRecorder.ts` | `startRecording` 调用 `startBattle`，`recordCombatRecord` 调用 `onAction` |
| `src/domain/battle/events/BattleEventManager.ts` | `handleBattleEndEvent` 调用 `onBattleEnd` + 发射 `battle-summary` 事件 |
| `src/presentation/views/BattleField.vue` | 监听事件，显示 `BattleSummaryDialog` |

**验证：** `npm run typecheck` → 0 新增错误

---

### 任务 1.6 — 全文本排版改造（Typography-First）

| 项目 | 内容 |
|------|------|
| **状态** | ✅ 已完成 |
| **设计原则** | 排版优先（Typography-First），纯文本信息架构。所有战斗反馈通过格式化文本（颜色、字重）表达，不依赖 Emoji/图标。 |
| **参考** | `documents/combat-log-system-design.md` 第 1.2 节核心原则 |



---

